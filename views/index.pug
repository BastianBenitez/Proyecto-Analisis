extends layout

block content 
    main
        section#hero
            h1 Bienvenido a Nombre del Sitio Web
            p Descripción breve del sitio web y lo que ofrece
            a(href="#", class="btn") Conoce más
            h1 Toneos
                .product-grid
                    each el in results 
                        .product
                            img(src="/img/img.jpeg", alt="img torneo 1")
                            h3 #{el.nombreTorneo}
                            p #{el.descripcion}
                            p Inicio: #{el.fechaInicioLegible} 
                            p Termino: #{el.fechaTerminoLegible}
                            a(href="#", class="btn") Más información
                    